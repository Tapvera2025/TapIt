import { useEffect, useState } from 'react';
import { createEmployee } from '../api/employeesApi.js';
import { getCompanyDepartments, getCompanyEmployees, getCompanyLadder, getCompanyTeams, type CompanyDepartment, type CompanyEmployee, type CompanyLadder, type CompanyTeam } from '../api/companyApi.js';

const blank = { fullName: '', email: '', departmentId: '', positionId: '', teamId: '', specialization: '', reportsTo: '' };

export function EmployeesPage(): React.JSX.Element {
  const [employees, setEmployees] = useState<CompanyEmployee[]>([]);
  const [departments, setDepartments] = useState<CompanyDepartment[]>([]);
  const [teams, setTeams] = useState<CompanyTeam[]>([]);
  const [ladder, setLadder] = useState<CompanyLadder | null>(null);
  const [form, setForm] = useState(blank);
  const [search, setSearch] = useState('');
  const [showCreate, setShowCreate] = useState(false);
  const [loading, setLoading] = useState(true);
  const [busy, setBusy] = useState(false);
  const [message, setMessage] = useState('');
  const [error, setError] = useState('');

  async function load(): Promise<void> { setLoading(true); try { const [nextEmployees, nextDepartments, nextTeams] = await Promise.all([getCompanyEmployees(), getCompanyDepartments(), getCompanyTeams()]); setEmployees(nextEmployees); setDepartments(nextDepartments); setTeams(nextTeams); } catch (cause) { setError(cause instanceof Error ? cause.message : 'Unable to load employees.'); } finally { setLoading(false); } }
  useEffect(() => { void load(); }, []);
  useEffect(() => { const department = departments.find((item) => item.id === form.departmentId); if (!department) { setLadder(null); return; } void getCompanyLadder(department.code).then(setLadder).catch(() => setError('Unable to load positions for this department.')); }, [form.departmentId, departments]);

  async function submit(event: React.FormEvent): Promise<void> { event.preventDefault(); setBusy(true); setError(''); setMessage(''); try { const result = await createEmployee({ fullName: form.fullName, email: form.email, departmentId: form.departmentId, positionId: form.positionId, ...(form.teamId ? { teamId: form.teamId } : {}), ...(form.specialization ? { specialization: form.specialization } : {}), reportsTo: form.reportsTo || null }); setMessage(`Employee ${result.employee.fullName} created. Credentials were sent by email.`); setForm(blank); setShowCreate(false); await load(); } catch (cause) { setError(cause instanceof Error ? cause.message : 'Unable to create employee.'); } finally { setBusy(false); } }
  const visible = employees.filter((employee) => employee.fullName.toLowerCase().includes(search.toLowerCase()));
  const departmentTeams = teams.filter((team) => team.departmentId === form.departmentId);

  return <div className="p-5 md:p-8"><div className="mx-auto max-w-7xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-app-accent">People</p><h1 className="mt-2 font-display text-3xl font-bold tracking-[-0.05em]">Employees</h1><p className="mt-2 text-sm text-app-muted">Manage employee accounts in your organization.</p></div><button type="button" onClick={() => setShowCreate((open) => !open)} className="rounded-lg bg-app-accent px-4 py-2.5 text-sm font-bold text-[#061412]">{showCreate ? 'Close form' : 'Create employee'}</button></div>{message && <p className="mt-5 rounded-xl border border-app-accent/30 bg-app-accent/10 p-4 text-sm text-app-accent">{message}</p>}{error && <p className="mt-5 rounded-xl border border-[#d86b6b]/30 bg-[#d86b6b]/10 p-4 text-sm text-[#d86b6b]">{error}</p>}{showCreate && <form onSubmit={(event) => { void submit(event); }} className="mt-6 grid gap-4 rounded-2xl border border-app-border bg-app-surface p-6 md:grid-cols-2"><Field label="Full name" value={form.fullName} onChange={(value) => setForm({ ...form, fullName: value })} required /><Field label="Email" type="email" value={form.email} onChange={(value) => setForm({ ...form, email: value })} required /><Select label="Department" value={form.departmentId} onChange={(value) => setForm({ ...form, departmentId: value, positionId: '', teamId: '' })} options={departments.map((item) => ({ value: item.id, label: item.name }))} required /><Select label="Position" value={form.positionId} onChange={(value) => setForm({ ...form, positionId: value })} options={(ladder?.positions ?? []).map((item) => ({ value: item.id, label: `${item.name} (${item.code})` }))} required /><Select label="Team (optional)" value={form.teamId} onChange={(value) => setForm({ ...form, teamId: value })} options={departmentTeams.map((item) => ({ value: item.id, label: item.name }))} /><Field label="Specialization (optional)" value={form.specialization} onChange={(value) => setForm({ ...form, specialization: value })} /><Select label="Reports to (optional)" value={form.reportsTo} onChange={(value) => setForm({ ...form, reportsTo: value })} options={employees.filter((item) => item.departmentId === form.departmentId).map((item) => ({ value: item.id, label: item.fullName }))} /><button type="submit" disabled={busy} className="rounded-lg bg-app-accent px-4 py-2.5 text-sm font-bold text-[#061412] disabled:opacity-50 md:col-span-2">{busy ? 'Creating...' : 'Create employee and send credentials'}</button></form>}{loading ? <div className="mt-6 h-56 animate-pulse rounded-2xl border border-app-border bg-app-surface" /> : visible.length === 0 ? <div className="mt-6 rounded-2xl border border-app-border bg-app-surface p-8 text-center"><h2 className="font-display text-xl font-bold">No employees found</h2><p className="mt-2 text-sm text-app-muted">Try another search or create an employee account.</p></div> : <section className="mt-6 overflow-hidden rounded-2xl border border-app-border bg-app-surface"><div className="border-b border-app-border p-4"><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search employees" className="w-full rounded-lg border border-app-border bg-app-background px-3 py-2.5 text-sm outline-none focus:border-app-accent" aria-label="Search employees" /></div><div className="divide-y divide-app-border">{visible.map((employee) => <div key={employee.id} className="grid gap-2 px-5 py-4 sm:grid-cols-[1.3fr_1fr_1fr_auto] sm:items-center"><div><p className="font-semibold">{employee.fullName}</p><p className="text-xs text-app-muted">Employee account</p></div><p className="text-xs text-app-muted">Department ID: {employee.departmentId ?? '—'}</p><p className="text-xs text-app-muted">Position ID: {employee.positionId ?? '—'}</p><span className="w-fit rounded-full bg-app-accent/15 px-2.5 py-1 text-xs font-bold text-app-accent">Active</span></div>)}</div></section>}</div></div>;
}

function Field({ label, value, onChange, type = 'text', required = false }: { label: string; value: string; onChange: (value: string) => void; type?: string; required?: boolean }): React.JSX.Element { return <label className="text-xs font-semibold text-app-muted"><span className="mb-2 block">{label}</span><input required={required} type={type} value={value} onChange={(event) => onChange(event.target.value)} className="w-full rounded-lg border border-app-border bg-app-background px-3 py-2.5 text-sm text-app-foreground outline-none focus:border-app-accent" /></label>; }
function Select({ label, value, onChange, options, required = false }: { label: string; value: string; onChange: (value: string) => void; options: Array<{ value: string; label: string }>; required?: boolean }): React.JSX.Element { return <label className="text-xs font-semibold text-app-muted"><span className="mb-2 block">{label}</span><select required={required} value={value} onChange={(event) => onChange(event.target.value)} className="w-full rounded-lg border border-app-border bg-app-background px-3 py-2.5 text-sm text-app-foreground outline-none focus:border-app-accent"><option value="">Select {label.toLowerCase()}</option>{options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label>; }
