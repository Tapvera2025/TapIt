/** Platform state-machine values shared by controllers/services. */
export type OrganizationStatus = 'active' | 'suspended';
export type InvitationStatus = 'pending' | 'accepted' | 'expired' | 'revoked';
